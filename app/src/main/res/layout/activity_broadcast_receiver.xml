<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.BroadcastReceiverActivity"
    android:background="#e7e6ff"
    android:padding="16dp">
    <ProgressBar
        android:id="@+id/pgb_battery"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_marginTop="60dp"
        android:layout_height="wrap_content" />


    <TextView
        android:id="@+id/txt_bettery"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/bettery_percentage"
        android:textSize="30dp"
        android:textStyle="bold"
        android:gravity="center"/>

    <ProgressBar
        android:id="@+id/pgb_btry"
        style="?android:attr/progressBarStyle"
        android:layout_below="@+id/pgb_battery"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</RelativeLayout>